import { Component } from "react";

class Header extends Component {
    render() {
        return(
            <header>
                <h1>CV Builder</h1>
            </header>
        )
    }
}

export default Header;
